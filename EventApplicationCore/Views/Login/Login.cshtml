@model EventApplicationCore.Model.LoginViewModel
@{
    Layout = "~/Views/Shared/_LoginLayout.cshtml";
}

<div class="login-box">
    <!-- /.login-logo -->
    <div class="card card-outline card-primary">
        <div class="card-header text-center">
            <a href="#" class="h1"><b>Event</b>ify</a>
        </div>
        <div class="card-body">
            <p class="login-box-msg h5">Sign in</p>
            @if (ViewBag.errormessage != null)
            {
                <p class="alert alert-danger" id="successMessage">@ViewBag.errormessage</p>
            }

            <form asp-controller="Login" asp-action="Login" method="post">
                <div class="input-group mb-3">
                    <input type="text" class="form-control" name="Username" placeholder="Username">
                    <div class="input-group-append">
                        <div class="input-group-text">
                            <span class="fas fa-envelope"></span>
                        </div>
                    </div>
                </div>
                <div class="input-group mb-3">
                    <input type="password" class="form-control" name="password" placeholder="Password">
                    <div class="input-group-append">
                        <div class="input-group-text">
                            <span class="fas fa-lock"></span>
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="col-8">
                        <div class="icheck-primary">
                            <input type="checkbox" id="remember">
                            <label for="remember">
                                Remember Me
                            </label>
                        </div>
                    </div>
                    <!-- /.col -->
                    <div class="col-4">
                        <input type="submit" class="btn btn-primary btn-block" value="Sign In" />
                    </div>
                    <!-- /.col -->
                </div>
            </form>

            <div class="social-auth-links text-center mt-2 mb-3">
                <a href="#" class="btn btn-block btn-primary">
                    <i class="fab fa-facebook mr-2"></i> Sign in using Facebook
                </a>
                <a href="#" class="btn btn-block btn-danger">
                    <i class="fab fa-google-plus mr-2"></i> Sign in using Google+
                </a>
            </div>
            <!-- /.social-auth-links -->

            <p class="mb-1">
                <a href="forgot-password.html">I forgot my password</a>
            </p>
            <p class="mb-0">
                @Html.ActionLink("Register a new membership","Registration","Registration",new{},new{ @class="text-center"})
                @*<a href="register.html" class="text-center">Register a new membership</a>*@
            </p>
        </div>
        <!-- /.card-body -->
        <div class="card-footer">
            <strong>Copyright &copy; @DateTime.Now.Year <a href="#" class="text-dark">Eventify</a>.</strong> All rights reserved.
        </div>
        <!-- /.card footer -->
        @*<footer class="main-footer">
            <!-- Default to the left -->
            <strong>Copyright &copy; @DateTime.Now.Year <a href="#" class="text-white">Eventify</a>.</strong> All rights reserved.
        </footer>*@
    </div>
    <!-- /.card -->
</div>
<!-- /.login-box -->


@*<div class="container" ng-controller="LoginController">
    <div class="box">
        <h2>
            Login
        </h2>
        @if (ViewBag.errormessage != null)
        {
            <p class="alert alert-danger" id="successMessage">@ViewBag.errormessage</p>
        }
        <form asp-controller="Login" asp-action="Login" method="post">
            @Html.AntiForgeryToken()
            <div class="inputBox">
                <input type="text" name="Username" required>
                <label>Username</label>
            </div>
            <div class="inputBox">
                <input type="password" name="password" required>
                <label>Password</label>
            </div>
            <input type="submit" name="submit" value="Login">
            <div class="signUp">
                No account ?  @Html.ActionLink("Sign Up", "Registration", "Registration")
            </div>
        </form>
    </div>
</div>*@

@*<div class="container" ng-controller="LoginController">
    <div class="panel panel-default">
        <div class="panel-heading">Login</div>
        <div class="panel-body">

            @if (ViewBag.errormessage != null)
            {
                <p class="alert alert-danger" id="successMessage">@ViewBag.errormessage</p>
            }

            <form asp-controller="Login" asp-action="Login" method="post">
                @Html.AntiForgeryToken()
                <div class="col-xs-offset-4 col-xs-4">

                    <div class="form-group">
                        <label class="control-label" asp-for="Username">Username</label>
                        <input autocomplete="off" asp-for="Username" type="text" class="form-control" />
                        <span asp-validation-for="Username" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label class="control-label" asp-for="Password">Password</label>
                        <input autocomplete="off" asp-for="Password" type="password" class="form-control" />
                        <span asp-validation-for="Password" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <input id="Submit1" class="btn btn-success" type="submit" value="Sign in To Account">
                    </div>
                    <div style="text-align: center;">
                        No account ?  @Html.ActionLink("Create one !", "Registration", "Registration")
                    </div>
                </div>
            </form>

        </div>
    </div>

</div>*@